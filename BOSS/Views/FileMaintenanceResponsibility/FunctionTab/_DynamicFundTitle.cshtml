@model BOSS.Models.FMmodels.FMResCenterModels.FunctionModel
@{
    BOSSEFConnectionString BOSSDB = new BOSSEFConnectionString();
}
@using BOSS.Models;

<div class="form-group">
    <label class="custom-form-margin">Fund</label>
    @Html.TextAreaFor(m => m.FundTitle, new { @class = "form-control", type = "text", @rows=1,  id = "FundTitle", @readonly = "@readonly" })
</div>
