
@model IEnumerable<BOSS.Models.FMPositionforSPModels.PositionList>
@{
    BOSSEFConnectionString BOSSDB = new BOSSEFConnectionString();
}
@using BOSS.Models;

<div class="row">
    <div class="col-1"></div>
    <div class="col-10">
        <table class="table table-responsive table-striped table-bordered table-condensed zero-configuration w-100 table-smss-hover">
            <thead class="text-center">
                <tr>
                    <th>Position Name</th>
                    <th>Position Code</th>
                    <th width="5%">Action</th>
                </tr>
            </thead>
            <tbody>
                @{
                    foreach (var Items in Model)
                    {
                        var DeleteConfirmationMess = "Are you sure do you want to delete this?";
                        var DeleteSuccesMess = "Successfully Deleted";

                        var UpdatePos = "<button class=\"btn btn-icon btn-block black btn-menuorange\" type=\"button\" onclick=\"GlobalEdit('Get_UpdatePosition','" + @Items.PositionID + "')\"> <i class='la la-edit'></i> Edit</button>";
                        var RemovePos = "<button class=\"btn btn-icon btn-block btn-menuRed\" type=\"button\" onclick =\"swalConfirmation('" + @DeleteConfirmationMess + "', '" + @Items.PositionTitle + "','" + @DeleteSuccesMess + "','GlobalDelete(" + @Items.PositionID + ",&quot;DeletePosition&quot;)')\"><i class='la la-trash'></i> Delete</button>";

                        var ActionPos = UpdatePos + RemovePos;
                        <tr>
                            <td>@Items.PositionTitle</td>
                            <td>@Items.PositionCode</td>
                            @*<td>
                                @{
                                    var PClassID = Items.PCID;
                                    var PositionClass = (from e in BOSSDB.PositionClassifications where e.PCID == PClassID select e).FirstOrDefault();
                                    var TDValue = "";
                                    if (PositionClass == null)
                                    {
                                        TDValue = "N/A";
                                    }
                                    else
                                    {
                                        TDValue = PositionClass.PCTitle;
                                    }
                                }
                                @TDValue
                            </td>*@
                            <td>
                                <button type="button" onfocusout="PopoveLostFocus()"
                                        class="btn btn-m btn-btnbackcolor popover-trigger"
                                        data-toggle="popover" data-content="@ActionPos" data-html="true" data-placement="left">
                                    <i class="la la-bars"></i>
                                </button>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
        <script>
            $(function () {
                LoadDataTable();
            })
        </script>
